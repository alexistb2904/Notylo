{
  "name": "@notylo/desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:desktop": "pnpm dev",
    "build": "tauri build",
    "build:windows": "tauri build --bundles nsis",
    "build:linux": "tauri build --bundles appimage,deb",
    "build:android": "tauri android build --apk --ci --target aarch64",
    "build:android:debug": "tauri android build --debug --apk --ci --target aarch64"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
